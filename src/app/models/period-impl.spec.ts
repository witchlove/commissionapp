/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {PeriodImpl} from './period-impl';

describe('PeriodImpl', () => {
  it('should create an instance', () => {
    expect(new PeriodImpl()).toBeTruthy();
  });
});
